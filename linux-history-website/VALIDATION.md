# 实际 Linux 验证

2026-09-07 UTC，在本机完成 torvalds/linux 全历史和完整 blob 下载后验证；没有使用浅克隆或合成数据。以下是单次开发机测量，供复现比较，不是生产延迟承诺。

- HEAD：`df2908090cda368b01ff43709f51890076c56157`
- `master`：1,482,108 个提交，33,650 位作者，945 个标签。
- 首提交：`1da177e4c3f41524e886b7f1b8a0c1fc7321cac2`，2005 年的 Linux Git 导入。
- 禁用懒取对象后，完整图索引初始化 18.06 秒；128 提交的远距离随机页 29.9–31.4 毫秒。
- 初期／中期／末期景观 390／869／651 毫秒；精确文件数分别为 17,291／62,911／94,856，每次仅传输 720 个采样文件。
- HEAD 精确变化页 338 毫秒，单文件补丁 26 毫秒。验证完毕 GC 后 Node heap 13.7 MiB、RSS 125.3 MiB，估算缓存保留 5.5 MiB；这些数值不包括独立原生 Git 子进程。

复现命令：

```sh
npm run prepare:data -- --full
GIT_NO_LAZY_FETCH=1 npm run verify -- --landscapes
npm test
npm run build
```

Playwright 使用真实本地站点验证：1440×1000 桌面和 390×844 窄屏均无横向溢出、任意本地路径入口、控制台异常或失败 HTTP 请求。连续拖动时间轴期间 0 次 API 请求，释放到提交索引 400,169 后只有 1 次前台历史页与 1 次景观请求，首次景观 574 毫秒；真实文件补丁正常渲染。

公共 HTTP 测试覆盖只读入口、路径隐藏、历史范围限制、禁止完整树大响应、响应与计算双条件背压、客户端断开和绝对形式 URL。数据脚本测试覆盖完整对象增量更新、partial-to-full 补齐、取消 Git 后代进程和拒绝修改现有工作目录。

## 渲染修复验证（0.2.1）

- 真实 Linux 前 7 次提交在 Canvas 组件中执行 29 组检查：正序、反序、往返跳转、Overview／Inspect、已采样及未采样文件选择、重载和窄屏。同一提交的几何结果完全一致，建筑碰撞、地块重叠、建筑越界和绘制深度逆序均为 0。
- 3952×2242／DPR 1 与 1976×1121／DPR 2 的静止背板均为 3952×2242；原先仅为 2719×1543。拖动、缩放和时间轴停止后恢复高清。Retina 场景平移／缩放绘图 JavaScript p95 均为 0.3 毫秒，未观察到超过 50 毫秒的长任务，空闲不重绘。
- 监测每一次 Canvas 尺寸分配，横竖屏切换等场景的 RGBA 像素峰值为 111.96 MiB，低于 128 MiB 预算；实际窄面板淡入只使用 8.71 MiB。该预算不包含浏览器、GPU 驱动等额外内存。DPR 切换、隐藏释放及恢复均通过。
- 更新后的真实网站仍覆盖 1,482,108 次提交。桌面与窄屏无页面错误或失败请求；拖动时间轴时 0 次 API 请求，释放后 1 次历史页、1 次景观请求。本轮释放到景观响应耗时 1,352 毫秒；服务端取数与画面交互耗时分别记录。
